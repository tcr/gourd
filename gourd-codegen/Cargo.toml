[package]
name = "gourd-codegen"
version = "0.1.0"
edition = "2024"
description = "Core Go to Rust transpilation library"
license = "MIT"

[lib]

[dependencies]
syn = { version = "2", features = ["full", "fold"] }
quote = "1"
proc-macro2 = "1"
